<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.edu.opet.pi">

    <uses-permission
        android:name="android.permission.WAKE_LOCK"
        tools:node="replace" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PI"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.WelcomeActivity"
            android:label="Perfil do Usuário"></activity>

        <receiver
            android:name=".receiver.Receiver"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".activities.ModifyTaskActivity"
            android:label="Editar Tarefa" />
        <activity
            android:name=".activities.AddTaskActivity"
            android:label="Adicionar Tarefa" />
        <activity
            android:name=".activities.CadastroActivity"
            android:label="Cadastro de Usuário" />
        <activity
            android:name=".activities.DashBoard"
            android:label="Minhas Tarefas" />
        <activity
            android:name=".activities.LoginCreation"
            android:label="Organiza Aí">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>