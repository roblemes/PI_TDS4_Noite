<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.edu.opet.pi">
    <uses-permission android:name="android.permission.WAKE_LOCK" tools:node="replace" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PI"
        android:usesCleartextTraffic="true">
        <activity android:name=".ModifyTaskActivity"></activity>
        <activity android:name=".AddTaskActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DashBoard" />
        <activity
            android:name=".LoginCreation"
            android:label="@string/title_activity_login_creation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>